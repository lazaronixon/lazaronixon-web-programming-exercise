<h1 class="hdg">
  Does this look right?
</h1>

<p class="flush--top">
  <strong>Your import is being processed.</strong> Please review these samples from the vCard to make sure it looks correct:
</p>

<section class="contact-list push--bottom">
  <% if @import.contacts.exists? %>
    <%= render partial: "contacts/contact", collection: @import.contacts %>
  <% else %>
    <p class="list--boxed">No new contacts were imported.</p>
  <% end %>
</section>

<%= link_to "Yes, looks good", account_contacts_path(@account), class: "btn" %>
<%= link_to "No, undo the import", account_contacts_import_path(@account, @import), "data-turbo-method": :delete, class: "btn" %>
